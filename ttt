
--select count(imie) as ilosc from kierowca where imie like '%a';
--select rocznik, AVG(spalanie) as srednio from samochody where rocznik > 2014 group by rocznik
--select marka,AVG(spalanie) as srednia from samochody where zasilanie = 'diesel' group by marka having avg(spalanie) < 7 order by marka
--select count(*) from samochody where marka like 'M%'
--select top 1 miasto,count(*) as ilosc from kierowca group by miasto order by ilosc desc

--select top 1 kolor, count( kolor) as liczba from samochody where zasilanie = 'lpg' group by kolor order by liczba desc
--select rocznik, AVG(cena) as srednia from samochody where kolor = 'czerwony' and marka = 'fiat' group by rocznik order by srednia desc
--select marka, COUNT(marka) as ilosc from samochody where kolor = 'czarny' group by marka having COUNT(marka) > 2
--select zasilanie, avg(spalanie) as srednia from samochody where zasilanie in ('benzyna','diesel') group by zasilanie
--select miasto, count(imie) as ilosc from kierowca group by miasto having count(imie) > 5 order by ilosc desc

--select zasilanie, min(spalanie) as spalanie from samochody group by zasilanie
--select * from samochody order by spalanie asc
