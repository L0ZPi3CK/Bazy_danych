każda kolumna ma zdefiniowane atrybuty które określają ograniczenia
Primary key
NOT NULL
IDENTITY
DEFAULT
UNIQUE
CHECK

Primary key
ALTER TABLE kliednt ADD primary key id_klienta

Jeśli w SQL do klucza podstawowego zostalo użyte ALTER TABLE kolumna klucza podstawowego musi mieć ustawione NOT NULL
Aby zabrac Primary key należyn w sql: 
--
--
--

NOT NULL 
oznacza ze nie moga wystapic wartosci puiste
Jak nie napiszemy nic to domyslnie jest puste.
ALTERTABLE KLIENT modify PESEL varchar(11) NOT NULL


IDENTITY/AUTO_INCREMENT
identity to automatyczne numerowanie
Nie można tego dodać do istniejącej kolumny

DEFAULT
wpowadza opcje domyślną, wprowadza to co jest po DEFAULT

ALTER TABLE KLIENT ALTER mijscowosc SET DEFAULT 'Krakow'


ALTER TABLE ... ADD CONSTRAINT ... CHECK( .... > ...);
ALTER TABLE ... DROP CONSTRAINT ...;
mysql -- ALTER TABLE ... DROP CHECK ...;


!!! INSERT !!!
!!! UPDATE !!!
!!! DELETE !!!

INSERT

INSERY INTO nazwatabeli (kolumna1int,kolumna2char,kolumna3money)
VALUES (kolumna1int_dane,kolumna2char_dane,kolumna3money_dane)
(kolumna1int_dane2,kolumna2char_dane2,kolumna3money_dane2)
(kolumna1int_dane3,kolumna2char_dane3,kolumna3money_dane3)

INSERT INTO ksiazki (tytul,cena)
values('PT',30)('Dziady',NULL)

UPDATE

UPDATE .... SET ...

UDATE nazwatabeli SET cena=25 where tytul='Dziady'

DELETE FROM KLIENT

DELETE FROM klient WHERE imie='Adam'


*create database biblioteka
go
use biblioteka;

create table ksiazki2
(
Id				int,
Tytul			varchar(25),	
Rok_wyd			int,
Liczba_stron	int,
ISBN			varchar(22)
);
*/
--ALTER TABLE ksiazki2 ALTER COLUMN Id int NOT NULL;
--ALTER TABLE ksiazki2 ADD CONSTRAINT ksiazki2Id_key primary key (Id);
--ALTER TABLE ksiazki2 ADD CONSTRAINT KsiazkiCheckRok CHECK (Rok_wyd >= 2012);
--ALTER TABLE ksiazki2 ADD CONSTRAINT ksiazki2setdefault DEFAULT 200 FOR Liczba_stron;
--ALTER TABLE ksiazki2 ADD CONSTRAINT Ksiazki2SetUniqueIsbn UNIQUE (ISBN);
--insert into ksiazki2 (Id, Tytul,ISBN) values (2,'Pan Tadeusz',44),(2137,'Kremufki',2137);
--select * from ksiazki2;
--update ksiazki2 set liczba_stron=300 where id=2;
--delete from ksiazki2 where id=2;
